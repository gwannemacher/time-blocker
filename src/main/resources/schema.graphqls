type Query {
  getTimeBlocks: [TimeBlock]
}

type Mutation {
    createTimeBlock(title: String!, type: String!, startTime: String!, startDate: String!, endTime: String!, endDate: String!, isAllDay: Boolean!): TimeBlock
    deleteTimeBlock(id: String!): [TimeBlock]
}

type TimeBlock {
    id: String
    title: String
    type: String
    startTime: String
    startDate: String
    endTime: String
    endDate: String
    isAllDay: Boolean
}